# WARNING: These versions are intentionally outdated for the scanning lab
  # In real life, always use the latest stable versions
  
Flask==1.0.2          # Multiple CVEs — use Flask==3.0.0 or later
requests==2.18.4      # CVE-2018-18074 — use requests==2.31.0 or later
cryptography==2.6     # Multiple CVEs in old versions
Pillow==8.0.0         # Multiple image processing CVEs
PyYAML==3.13          # CVE-2017-18342 — arbitrary code execution
Werkzeug==0.14.1      # CVE-2019-14806 — path traversal
SQLAlchemy==1.3.0
celery==4.3.0
